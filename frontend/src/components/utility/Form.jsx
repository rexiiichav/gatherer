export default function Form({ children }) {
  return (
    <div class="p-15 h-full w-full flex items-center justify-center bg-gradient-to-r from-teal-200 to-lime-200">
      {children}
    </div>
  );
}
